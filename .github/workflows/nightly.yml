name: pages-manual

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Actions nightly automation is disabled; OpenClaw local cron is the source of truth."
